/**
    Export filters for FidoCadJ drawings. They are implementations of the
    ExportGraphic interface. The class ExportGraphic contains some general
    use methods related to the export, but not only (such as calculating
    image size and so on).
*/

package fidocadj.export;